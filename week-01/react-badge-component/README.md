#  Reusable React Component

This project demonstrates a simple reusable React component using props.

## Description
A `Badge` component is created and reused multiple times with different values to show component reusability.

## Technologies Used
- React
- Vite

## How to Run
1. Install dependencies:
   npm install
2. Start the development server:
   npm run dev
